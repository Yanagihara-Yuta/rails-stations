<main>
    <table>
        <tr>
            <th><%= @da %></th>
            <th></th>
            <th>スクリーン</th>
            <th></th>
            <th></th>
        </tr>
        <tr>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
        <% for i in 0..@row.length do %>
            <tr>
            <%= @row[i] %>
            <% row = @row[i] %>
            <% @sheet_select_row = Sheet.where(row: row) %>
            <% @sheet_select_row.each do | sheet | %>
            <th>
                <%= link_to("/movies/#{@movie_id}/schedules/#{@schedule_id}/reservations/new?date=#{@date}&sheet_id=#{sheet.id}") do %>
                <%= sheet.row %>-<%= sheet.column %>

                <% end %>
            </th>
            <% end %> 
            </tr>
        <% end %>
    </table>
</main>